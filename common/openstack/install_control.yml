---
  - hosts: openstack_control
    vars:
      target_playground: k-post
      openstack_distro: queens
      mysql_root_password: openstack
      rabbit_pass: openstack
    include_roles: basic_env
    include_roles: keystone
